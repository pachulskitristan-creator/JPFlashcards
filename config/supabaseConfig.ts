// config/supabaseConfig.ts
//
// These values are safe to keep in source code — the "publishable" key
// is specifically designed to be embedded in client apps (same idea as
// a Stripe publishable key). It only allows the operations your Supabase
// project's Row Level Security policies permit; it is NOT the secret/
// service-role key, which must never appear in app code.

export const SUPABASE_URL = 'https://smzlrffxbgnhtvuxkfff.supabase.co';
export const SUPABASE_PUBLISHABLE_KEY = 'sb_publishable_4Z1I5-L-tO53iYWUDst-Aw_yM0XoZsq';
